@page
@model RazorPagesMovie.Pages_Actors.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Actor</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Actor.Id" />
            <div class="form-group">
                <label asp-for="Actor.FullName" class="control-label"></label>
                <input asp-for="Actor.FullName" class="form-control" />
                <span asp-validation-for="Actor.FullName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Actor.Birth" class="control-label"></label>
                <input asp-for="Actor.Birth" class="form-control" />
                <span asp-validation-for="Actor.Birth" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Actor.MovieId" class="control-label"></label>
                <select asp-for="Actor.MovieId" class="form-control" asp-items="ViewBag.MovieId"></select>
                <span asp-validation-for="Actor.MovieId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
